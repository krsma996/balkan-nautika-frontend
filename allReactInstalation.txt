1.) Koristim tailwind 
npm install tailwindcss @tailwindcss/vite
konfigurises ga tamo sa ovim sranjima u vite.config
import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'
export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
})

2.)npm install -g npm@11.2.0
3.)npm install @headlessui/react @heroicons/react
4.)npm install swiper@6.8.4
5.)npm install @fortawesome/free-solid-svg-icons











import { FaShip, FaMoneyBillWave, FaMapMarkerAlt, FaSearch } from "react-icons/fa";

export const SearchBoatForm = () => {
  return (
    <div className="bg-gradient-to-r from-gray-100 to-gray-200 py-12">
      <div className="max-w-7xl mx-auto px-4">

        <div className="bg-white rounded-2xl shadow-xl border border-gray-200 p-8">

          {/* NASLOV */}
          <h2 className="text-2xl font-semibold text-gray-800 mb-4 text-center">
            Pronađi idealno plovilo
          </h2>

          {/* QUICK FILTERS */}
          <div className="flex flex-wrap justify-center gap-3 mb-8">
            {["Gliseri", "Do 10.000€", "Novi oglasi", "Jedrilice"].map((item, i) => (
              <button
                key={i}
                className="px-4 py-2 bg-gray-100 rounded-full text-sm hover:bg-gray-200 transition"
              >
                {item}
              </button>
            ))}
          </div>

          {/* FORMA */}
          <form className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">

            {/* TIP */}
            <div>
              <label className="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
                <FaShip /> Tip plovila
              </label>
              <select className="w-full rounded-lg border border-gray-300 focus:border-black focus:ring-0">
                <option>Sva plovila</option>
                <option>Čamac</option>
                <option>Jahta</option>
                <option>Gliser</option>
                <option>Katamaran</option>
              </select>
            </div>

            {/* CENA */}
            <div>
              <label className="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
                <FaMoneyBillWave /> Cena (€)
              </label>
              <input
                type="number"
                placeholder="Do 50.000€"
                className="w-full rounded-lg border border-gray-300 focus:border-black focus:ring-0"
              />
            </div>

            {/* REGION */}
            <div>
              <label className="flex items-center gap-2 text-sm font-medium text-gray-700 mb-1">
                <FaMapMarkerAlt /> Region
              </label>
              <select className="w-full rounded-lg border border-gray-300 focus:border-black focus:ring-0">
                <option>Svi regioni</option>
                <option>Dunav</option>
                <option>Jadran</option>
                <option>Mediteran</option>
              </select>
            </div>

            {/* GODIŠTE */}
            <div>
              <label className="text-sm font-medium text-gray-700 mb-1 block">
                Godište od
              </label>
              <input
                type="number"
                placeholder="npr. 2015"
                className="w-full rounded-lg border border-gray-300 focus:border-black focus:ring-0"
              />
            </div>

            {/* DUGME */}
            <div className="col-span-1 md:col-span-2 lg:col-span-4 flex justify-center mt-6">
              <button
                type="submit"
                className="px-12 py-4 rounded-xl bg-[#222831] text-white font-semibold
                           hover:bg-black transition-all shadow-lg flex items-center gap-3 text-lg"
              >
                <FaSearch />
                Pretraži plovila
              </button>
            </div>

          </form>

          {/* FOOTER HINT */}
          <p className="text-center text-sm text-gray-500 mt-6">
            Pretražite oglase plovila, opreme i nautičkih usluga
          </p>

        </div>
      </div>
    </div>
  );
};